function multiplyNums(x, y, z){
    return "new number is " + x * y * z;
}

console.log(multiplyNums(1, 2, 3));

multiplyNumsArray = (x, y, z) => {
    return "new number is " + x * y * z;
}

console.log(multiplyNumsArray(1,2,4));